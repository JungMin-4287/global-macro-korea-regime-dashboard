DASH.fixForeignCards=()=>{const foreign=DASH.data?.cycle_signals?.foreign||{},metrics=document.getElementById('foreignCycleMetrics'),text=document.getElementById('foreignCycleText'),f=DASH.f;if(!metrics)return;const snap=foreign.market_snapshot||{},has=snap.kospi_foreign_ownership_mcap_weighted_pct!=null,fourth=has?`<div class="metric"><span class="muted">KOSPI 외국인 보유 비중</span><b>${f(snap.kospi_foreign_ownership_mcap_weighted_pct)}%</b><small>${snap.date||'-'} 확정치</small></div>`:`<div class="metric"><span class="muted">최근 5일 외국인 순매수</span><b>${f(foreign.net_buy_5d_trn)}조원</b><small>KOSPI 보유비중 미산출 시 대체</small></div>`;metrics.innerHTML=`<div class="metric"><span class="muted">최근 20일 순매수</span><b>${f(foreign.net_buy_20d_trn)}조원</b><small>5일 ${f(foreign.net_buy_5d_trn)}조원</small></div><div class="metric"><span class="muted">삼성 외국인 지분율</span><b>${f(foreign.samsung_foreign_ownership_pct)}%</b><small>20일 ${f(foreign.samsung_foreign_ownership_20d_change_pp)}%p</small></div><div class="metric"><span class="muted">하이닉스 외국인 지분율</span><b>${f(foreign.skhynix_foreign_ownership_pct)}%</b><small>20일 ${f(foreign.skhynix_foreign_ownership_20d_change_pp)}%p</small></div>${fourth}`;if(text){const status=text.querySelector('.status-line'),caveat=has?`KOSPI 외국인 보유비중은 ${snap.date||'-'} 확정치입니다.`:'KOSPI 전체 외국인 보유비중은 이번 실행에서 얻지 못해 5일 순매수로 대체했습니다.';if(status)status.innerHTML=`${foreign.source||'원자료 미확인'}<br>${caveat} 삼성전자·SK하이닉스 지분율은 확정치 시차가 있을 수 있습니다.`}};
DASH.render=()=>{const D=DASH.data;document.getElementById('generatedAt').textContent=D.generated_at||'-';const order=['KOSPI','KOSDAQ','SAMSUNG','SKHYNIX','SOX','NDX','VKOSPI'];document.getElementById('summary').innerHTML=order.filter(k=>D.assets?.[k]).map(k=>DASH.card(D.assets[k])).join('');['KOSPI','KOSDAQ','SAMSUNG','SKHYNIX','SOX'].forEach(k=>{if(D.assets?.[k])DASH.renderAsset(k,D.assets[k])});DASH.renderPsychology(D.market_psychology||{});DASH.renderEnv();DASH.renderCycleSignals();DASH.fixForeignCards();DASH.renderStudy();DASH.renderVal();DASH.renderStress();DASH.renderOther();DASH.renderErrors()};
fetch('data/market_data.json',{cache:'no-store'}).then(r=>r.ok?r.json():Promise.reject(new Error('market_data fetch failed'))).then(data=>{DASH.data=data;DASH.render()}).catch(()=>DASH.render());
