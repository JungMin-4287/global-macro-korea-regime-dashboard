DASH.renderAsset=(k,a)=>{const C=DASH.C,h=(a.history||[]).slice(-260),l=h.map(x=>x.date),s=DASH.s,line=DASH.line;if(!h.length)return;
if(['KOSPI','KOSDAQ'].includes(k)){line(k+'-price',l,[{label:a.name,data:s(h,'close'),borderColor:C.p,pointRadius:0,borderWidth:2},{label:'50일선',data:s(h,'sma50'),borderColor:C.b,pointRadius:0},{label:'120일선',data:s(h,'sma120'),borderColor:C.c,pointRadius:0}]);const refs=k==='KOSPI'?[102,93,91]:[95,93,80,77];line(k+'-ratio',l,[{label:'50일 이격도',data:s(h,'ratio50'),borderColor:C.w,pointRadius:0,borderWidth:2},{label:'120일 이격도',data:s(h,'ratio120'),borderColor:C.g,pointRadius:0,borderWidth:2},{label:'100',data:l.map(()=>100),borderColor:C.p,borderDash:[5,5],pointRadius:0},...refs.map((v,i)=>({label:'역사 참고 '+v,data:l.map(()=>v),borderColor:[C.a,C.w,C.r,C.c][i%4],borderDash:[3,5],borderWidth:1,pointRadius:0}))]);DASH.setInterpretation(k+'-interpretation',a)}
else if(['SAMSUNG','SKHYNIX'].includes(k)){line(k+'-price',l,[{label:a.name,data:s(h,'close'),borderColor:C.p,pointRadius:0,borderWidth:2},{label:'30일선',data:s(h,'sma30'),borderColor:C.a,pointRadius:0},{label:'50일선',data:s(h,'sma50'),borderColor:C.b,pointRadius:0},{label:'120일선',data:s(h,'sma120'),borderColor:C.c,pointRadius:0}]);line(k+'-ratio',l,[{label:'30일 이격도',data:s(h,'ratio30'),borderColor:C.w,pointRadius:0,borderWidth:2},{label:'50일 이격도',data:s(h,'ratio50'),borderColor:C.b,pointRadius:0},{label:'100',data:l.map(()=>100),borderColor:C.p,borderDash:[5,5],pointRadius:0},{label:'110',data:l.map(()=>110),borderColor:C.r,borderDash:[5,5],pointRadius:0},{label:'115',data:l.map(()=>115),borderColor:C.c,borderDash:[3,5],pointRadius:0}]);DASH.setInterpretation(k+'-interpretation',a)}
else if(k==='SOX'){line('SOX-price',l,[{label:'SOX',data:s(h,'close'),borderColor:C.p,pointRadius:0,borderWidth:2},{label:'50일선',data:s(h,'sma50'),borderColor:C.b,pointRadius:0},{label:'100일선',data:s(h,'sma100'),borderColor:C.g,pointRadius:0},{label:'200일선',data:s(h,'sma200'),borderColor:C.w,pointRadius:0}]);line('SOX-ratio',l,[{label:'50일 이격도',data:s(h,'ratio50'),borderColor:C.b,pointRadius:0},{label:'100일 이격도',data:s(h,'ratio100'),borderColor:C.g,pointRadius:0},{label:'200일 이격도',data:s(h,'ratio200'),borderColor:C.w,pointRadius:0},{label:'100',data:l.map(()=>100),borderColor:C.p,borderDash:[5,5],pointRadius:0}]);DASH.setInterpretation('SOX-interpretation',a)}};

DASH.renderPsychology=p=>{const e=document.getElementById('psychology'),points=p.points||[],reg=p.regression||[],highlights=p.highlights||[],f=DASH.f,C=DASH.C,metrics=document.getElementById('psychologyMetrics'),text=document.getElementById('psychologyText');
if(!points.length){if(e&&e.parentElement)e.parentElement.innerHTML='<div class="note bad-note"><b>수급 데이터 미산출</b><br>KRX와 네이버 금융을 순차 조회했지만 가격 데이터와 결합할 수 있는 개인 순매수 시계열을 받지 못했습니다.</div>';metrics.innerHTML='<div class="metric"><span class="muted">현재 구간</span><b>미산출</b></div><div class="metric"><span class="muted">개인 순매수</span><b>-</b></div><div class="metric"><span class="muted">KOSPI 수익률</span><b>-</b></div><div class="metric"><span class="muted">회귀 잔차</span><b>-</b></div>';text.innerHTML=`<b>현재 해석:</b> ${p.interpretation||'개인 순매수 데이터 미산출'}<div class="status-line">${p.source||p.error||'다음 자동 실행에서 재시도합니다.'}</div>`;return}
new Chart(e,{type:'scatter',data:{datasets:[
{label:'일별 관측치',data:points.map(x=>({x:x.x,y:x.y,date:x.date})),backgroundColor:'rgba(160,180,205,.68)',borderColor:'rgba(110,130,155,.85)',pointRadius:3.5},
{label:'회귀선',type:'line',data:reg,borderColor:C.a,borderDash:[6,5],borderWidth:2,pointRadius:0},
{label:'최근',data:points.slice(-1).map(x=>({x:x.x,y:x.y,date:x.date})),backgroundColor:C.w,borderColor:'#9b6b00',borderWidth:2,pointRadius:8},
{label:'주요 변곡',data:highlights.map(x=>({x:x.x,y:x.y,label:x.label,date:x.date})),backgroundColor:'#ff9d3f',borderColor:'#4b2a00',borderWidth:2,pointRadius:7}
]},options:{responsive:true,maintainAspectRatio:false,interaction:{mode:'nearest',intersect:false},plugins:{legend:{labels:{color:'#c8d2e8',boxWidth:18}},quadrantBackground:{enabled:true},pointLabels:{enabled:true,datasetIndex:3},tooltip:{callbacks:{label:ctx=>{const d=ctx.raw||{};return `${d.date||ctx.dataset.label}: 개인 ${f(d.x)}조원 · KOSPI ${f(d.y)}%`}}}},scales:{x:{title:{display:true,text:'개인 순매수(조원)',color:'#95a3bd'},ticks:{color:'#95a3bd'},grid:{color:'rgba(43,59,94,.3)'}},y:{title:{display:true,text:'KOSPI 수익률(%)',color:'#95a3bd'},ticks:{color:'#95a3bd'},grid:{color:'rgba(43,59,94,.3)'}}}}});
const z=p.latest||{};metrics.innerHTML=`<div class="metric"><span class="muted">현재 구간</span><b>${p.zone||'미산출'}</b></div><div class="metric"><span class="muted">개인 순매수</span><b>${f(z.x)}조원</b></div><div class="metric"><span class="muted">KOSPI 수익률</span><b>${f(z.y)}%</b></div><div class="metric"><span class="muted">회귀 잔차</span><b>${f(z.residual)}%p</b></div>`;text.innerHTML=`<b>현재 해석:</b> ${p.interpretation||'미산출'}<div class="status-line">상관계수 ${f(p.correlation)} · 기준일 ${z.date||'-'} · ${p.source||'-'}<br>${p.highlight_note||''}</div>`};

DASH.renderEnv=()=>{const D=DASH.data,C=DASH.C,s=DASH.s,line=DASH.line,f=DASH.f,nd=D.assets?.NDX,v=D.assets?.VKOSPI,k=D.assets?.KOSPI;
if(nd){const h=nd.history.slice(-260),l=h.map(x=>x.date);line('NDX-price',l,[{label:'나스닥100',data:s(h,'close'),borderColor:C.p,pointRadius:0,borderWidth:2},{label:'100일선',data:s(h,'sma100'),borderColor:C.a,pointRadius:0}]);DASH.setInterpretation('ndxText',nd)}else document.getElementById('ndxText').innerHTML='나스닥100 데이터 미산출';
if(v){const h=v.history.slice(-260),l=h.map(x=>x.date);line('VKOSPI-price',l,[{label:v.is_proxy?'KOSPI 20일 실현변동성(대체)':'VKOSPI',data:s(h,'close'),borderColor:C.r,pointRadius:0,borderWidth:2},{label:'20일 평균',data:s(h,'sma20'),borderColor:C.w,pointRadius:0},{label:'50일 평균',data:s(h,'sma50'),borderColor:C.a,pointRadius:0}]);DASH.setInterpretation('vkospiText',v)}else document.getElementById('vkospiText').innerHTML='<div class="headline">변동성 미산출</div><div>실제 VKOSPI 조회와 KOSPI 실현변동성 대체 계산이 모두 실패했습니다.</div><div class="action"><b>해석:</b> 변동성 확인 전에는 과매도 매수 비중을 제한합니다.</div>';
if(k){const h=k.history.slice(-520),l=h.map(x=>x.date);line('KOSPI-ratio60',l,[{label:'60일 이격도',data:s(h,'ratio60'),borderColor:C.a,pointRadius:0,borderWidth:2},{label:'100',data:l.map(()=>100),borderColor:C.p,borderDash:[5,5],pointRadius:0},{label:'과거 비교 90',data:l.map(()=>90),borderColor:C.w,borderDash:[5,5],pointRadius:0}]);const r=k.ratio60,zone=r==null?'미산출':r<85?'2018년 이후 극단적 과매도':r<90?'역사적 과매도':r<100?'60일선 하회 조정':'60일선 위 중기 추세',msg=r<90?'과거 저점권이지만 낙하 중이면 추가 하락할 수 있습니다. VKOSPI 고점 통과와 전저점 방어를 함께 확인합니다.':r<100?'중기 조정 국면입니다. 100선 회복 전까지 반등 지속성을 낮게 봅니다.':'중기 추세는 유지되지만 110 이상이면 과열을 별도 점검합니다.';document.getElementById('kospi60Text').innerHTML=`<div class="headline">현재 구간: ${zone}</div><div>60일 이격도 ${f(r)} (${r==null?'-':f(r-100)}%).</div><div class="action"><b>해석:</b> ${msg}</div>`}};